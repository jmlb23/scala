//podemos usar estructuras iterativas da programacion imperativa
//xa que scala non nos forza a usar o estilo funcional

//inferencia do tipo
//non implica que scala teña un tipado forte e statico
var i = 0

while(i < args.length){
  println(args(i))
  //notar que non existe nos enteiros o ++ xa que vai encontra da filosofia funcional
  i += 1
}
